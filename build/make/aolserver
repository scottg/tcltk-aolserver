#!/bin/bash

if [ -f nsconfig.tcl ]; then
    $TCLSH_CMD nsconfig.tcl -install $SITEROOT/exe/$NAME -debug
else
    ./configure \
        --prefix=$SITEROOT/exe/aolserver \
        --with-tcl=$SITEROOT/exe/tcl/lib
fi

make
make install

